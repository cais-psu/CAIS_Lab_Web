name: on-schedule

on:
  schedule:
    - cron: "0 3 1,15 * *"  # 每月 1 / 15 日 03:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  call-update:
    uses: cais-psu/CAIS_Lab_Web/.github/workflows/update-citations.yaml@main
    # ⭐ 关键：把 secrets 继承给被调用工作流
    secrets: inherit
    with:
      open-pr: true
